@model ReflectionIT.Mvc.Paging.PagingList<Mozzafiato.Models.Pedido>
@{
    ViewData["Title"] = "Index";
}

<h1>Pedidos</h1>

<form method="get" class="form-inline">

    <input name="filter" class="form-control" placeholder="filtro"

           value="@Model.RouteValue["filter"]" />
    <br />
    <button type="submit" class="btn btn-info">
        <span class="glyphicon glyphicon-search" aria-hidden="true"></span> Procurar
    </button>
</form>

<div class="align-content-center">
    Total de pedidos: @Model.TotalRecordCount
</div>
<div>
    <vc:pager paging-list="@Model" display-previous-next="true" /> @* nao mudo nada esse previous next bixo *@
</div>
   @*  <a asp-action="Create"> Criar novo pedido </a> n faz sentido criar pedido sem ser cliente neh fi*@
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Endereco2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cep)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PedidoEnviado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PedidoEntregueEm)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Endereco2)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Cep)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Cidade)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Estado)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Telefone)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PedidoEnviado)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PedidoEntregueEm)
                </td>
                <td>

                    <a asp-action="Edit" asp-route-id="@item.PedidoId">Editar</a> |
                    <a asp-action="Details" asp-route-id="@item.PedidoId">Detalhes</a> |
                    <a asp-action="Delete" asp-route-id="@item.PedidoId">Excluir</a> |
                    <a asp-action="PedidoLanches" asp-route-id="@item.PedidoId">Ver detalhes</a>
                </td>
            </tr>
        }
    </tbody>
</table>
